apiVersion: v1
kind: Service
metadata:
  name: {{ include "google-ads-mcp.fullname" . }}
  labels:
    {{- include "google-ads-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google-ads-mcp.selectorLabels" . | nindent 4 }}
