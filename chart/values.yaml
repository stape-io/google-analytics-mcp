image:
  repository: ghcr.io/stape-io/google-analytics-mcp
  pullPolicy: IfNotPresent
  tag: ""

resources:
  requests:
    cpu: 10m
    memory: 200Mi

autoscaling:
  enabled: false

host: "example.com"
